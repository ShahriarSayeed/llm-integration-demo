// Placeholder — regenerate with: `npx supabase gen types typescript --local > src/integrations/supabase/types.ts`
// After migration `20260405120000_chemical_reading_default_value`, `chemical_reading_configs` includes `default_value` (text, nullable).
// `demo_verification_codes` — CRM demo 2FA codes (Zapier webhook + `DemoLoginCard` poll); use `as any` until regenerated.
// Sales demo tables `demo_leads`, `demo_tour_videos`: use `.from("demo_tour_videos" as any)` until regenerated.
// Tipping: `technician_tip_profiles`, `tip_requests` — use `.from("tip_requests" as any)` until regenerated.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
